<div class='navLinks'>
    Home
</div>
{{!-- <div class="userContainer">
    <div class="users">
        {{#each users}}
        <a href="/users/{{this._id}}" class="link user">
            <div class="userImage">
                {{#if this.photoUrl}}
                <img src="{{this.photoUrl}}" alt="{{this.username}}'s profile photo"> {{else}}
                <img src="https://i.imgur.com/3ggmLxE.jpg" alt="{{this.username}}'s profile photo"> {{/if}}
            </div>
            <div class="userName">
                {{this.username}}
            </div>
        </a>
        {{/each}}
    </div> --}}

    <div class="logForm">

    <h2>Log In</h2>
    <div>
        <form action="/users" method="POST">
            <div class="form-input">
                <label for="logemail">email</label>
                <input type="text" name="logemail" required/>
            </div>

            <div class="form-input">
                <label for="logpassword">password</label>
                <input type="password" name="logpassword" required/>
            </div>

            <div class="button-container flex-end">
                <input class="btn btn-primary" type="submit" value="Log In">
                <a href="/users" class="btn btn-warning">Cancel</a>
            </div>
        </form>
    </div>
</div>

    <div class="btn btn-success">
        <a href="/users/new">
            Register User
        </a>
    </div>
</div>